<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.miser.little.mapper.UserMapper">
    <sql id="paramSql">
        where 1=1
        and user_name = #{requestDto.userName}
    </sql>

    <select id="selectUserList" resultType="com.miser.little.dto.user.UserListResponseDto">
        select
        *
        from char_user
        <include refid="paramSql"/>
    </select>
</mapper>